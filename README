


	*    hide
	*    show
	*    toggle
	*    css
	*    opacity
	*    html
	*    ready
	*	 click
	*	 dblclick
	*	 contextmenu
	*	 blur
	*	 hover
	*	 mouseover
	*	 mouseout
	*	 mousedown
	*	 mouseup
	*	 mousemove
	*	 mousewheel
	*	 keydown
	*	 keyup
	*	 keypress
	*	 cut
	*	 copy
	*	 paste
	*	 change
	*	 focus
	*	 scroll
	*	 reset
	*	 select
	*	 submit
	*	 resize




    dom(document).ready(function() {

        dom(find(".myclass"))
                .hide()
                .show()
                .css({
                    color:"red"
                })
                .html("Q")
                .on("click", function(x) {
                    dom(this).css({
                        backgroundColor: "green"
                    });
                });

        dom(find("#myid"))
                .on("click", function(x) {
                    dom(this).opacity(.5);
                });
    });



